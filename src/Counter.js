import React, { useState } from 'react';

const Counter = () => { 
  const[counter, State] = useState(0);
  const[counter, State] = useState(0);
  return (
    <>
    <h2>{counter}</h2>
    Hello World
    </>
  )
}
export default Counter;
